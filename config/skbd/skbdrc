# quickly lock the screen.
ctrl + shift - s: lock

# launch a new iTerm2 window, whether it is open or not.
# ctrl + shift - return: if ! osascript -e 'tell application "iTerm2" to create window with default profile' > /dev/null; then open -a iTerm; fi

# launch a new Alacritty window, whether it is open or not.
ctrl + shift - return: if ! alacritty msg create-window > /dev/null; then open -a Alacritty; fi
