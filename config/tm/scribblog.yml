root: ~/Code/scribblog

sessions:
  - name: scribblog
    root: ./api
    windows:
      - name: server
        commands:
          - make watch
      - name: editor
        commands:
          - nvim .

  - name: scribblog-web
    root: ./web
    windows:
      - name: server
        commands:
          - npm run dev
      - name: storybook
        commands:
          - npm run storybook
      - name: tests
        commands:
          - npm test
      - name: editor
        commands:
          - nvim .
