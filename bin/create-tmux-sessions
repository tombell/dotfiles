#!/usr/bin/env bash
set -e

components=(
  backstage
  contractor
  doorbell
  foundations
  frontdoor
  knock-knock
  letterbox
  living
  marketing
  neighbourhood
  piggybank
  switchboard
)

for c in "${components[@]}"; do
  cd "$HOME/Code/$c" && tmux new-session -d -s "$c"
done
