alias l="ls -lAh"
alias ll="ls -al"
alias la="ls -A"
alias e="$VISUAL"
alias v="$VISUAL"

# pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# make sure vim runs neovim
alias vim="nvim"
